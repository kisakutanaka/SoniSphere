# Step.md

SoniSphereの実装ロードマップ。「動くものが一つ増える」粒度でフェーズを分割する。
各フェーズの最後には実機確認ステップ（デプロイ→実機でアクセス→確認）を入れる。
実装後は各ステップに一言メモ（何を作ったか、何が分かったか）を書き足していく。

## Phase 0: 環境構築

- [x] TypeScript + ビルドツール（Vite等）の最小構成を作成
- [x] GitHub Pagesへのデプロイ導線を確認（空ページでよいので一度通す）
- [x] 実機確認: デプロイ→実機（スマホ含む）でアクセスできることを確認

メモ: Vite + vanilla TypeScript + npmで構成。GitHub Actions
（`.github/workflows/deploy.yml`）でmainへのpushを起点にGitHub Pagesへ自動デプロイ。
リポジトリ`kisakutanaka/SoniSphere`、公開URLは`https://kisakutanaka.github.io/SoniSphere/`
（`vite.config.ts`の`base`をリポジトリ名に合わせて設定）。

## Phase 1: データ前処理パイプライン（縦の筋・第一歩）

- [x] `gaia_catalog/bright_stars_v3.5.csv`を読み、少数（最も明るい10〜20個程度）の
      星に絞って軽量JSONへ変換するビルド時スクリプトを作成
- [x] 変換後のJSONの形式（ra/dec/vmag/bv→アプリで使う座標・パラメータ）を決定
- [x] 実機確認: 生成したJSONがアプリから読み込めることを確認

メモ: `scripts/build-catalog.mjs`（`npm run build:catalog`）でCSV→
`public/data/stars.json`を生成。vmag昇順（明るい順）で上位20件のみを対象とする
プロトタイプ用の絞り込み（Phase 7でフルカタログへ拡張予定）。
各星は`{id, ra, dec, vmag, bv, dir:[x,y,z]}`の形式で、`dir`は赤経・赤緯から
変換した天球上の単位ベクトル（dec=+90度をz=+1とする）。最も明るい星（vmag=-1.46,
ra≈101.29, dec≈-16.72）がシリウスの実際の座標と一致することを確認済み。
`main.ts`から`fetch(import.meta.env.BASE_URL + 'data/stars.json')`で読み込み、
件数を画面表示する形でプレビューサーバー上での疎通を確認した。

## Phase 2: 3D天球の可視化

- [x] 少数の星を全天球上（3D）に配置して表示する最小実装
- [x] マウス/タッチでの視点操作
- [x] 実機確認: デプロイ→実機で星の配置と視点操作を確認

メモ: Three.jsを採用。カメラを原点付近に固定し、`src/look-controls.ts`の
独自ドラッグ操作（Pointer Eventsでマウス/タッチを統一的に処理）でyaw/pitchを
回転させる方式にした（外部の対象を周回するOrbitControlsとは逆の「天球の内側から
見回す」動き）。この方式はPhase 8の端末向き（DeviceOrientation）連携にも
そのまま拡張しやすい。星はvmagに応じてサイズを線形マッピングした小球体
（`MeshBasicMaterial`）で表現（Phase 4で明るさ・色の音響マッピングと合わせて
見直す可能性あり）。Playwrightでdevサーバーを起動しヘッドレスブラウザで検証:
星の描画、ステータス文言表示、ドラッグによる視点回転（星の画面上の位置が変化）を
スクリーンショットで確認し、コンソールエラーもなし。GitHub Pages実機確認は
Phase 1の変更と合わせてpush後に実施。

## Phase 3: Web Audio APIによる3D音響（基本）

- [x] 単一の星の方向から音が聞こえる最小実装（Web Audio APIの3Dパンニング）
- [x] ヘッドホンでの方向定位が実際に機能しているかを確認
- [x] 実機確認: デプロイ→実機・ヘッドホンで方向定位を確認

メモ: `src/audio.ts`の`SpatialAudio`クラスでWeb Audio APIの`PannerNode`
（`panningModel: 'HRTF'`、正しい仰角定位に必要）と`AudioListener`を使用。
リスナーの位置は原点固定、向き（forward/up）はThree.jsカメラのクォータニオンから
毎フレーム算出して反映（`scene.ts`の`onTick`フック経由）。音源はサンプル音声を
使わず、非整数倍音のサイン波4本を短いエンベロープで鳴らすベル/グロッケン風の
その場合成（`BELL_PARTIALS`）。ブラウザの自動再生制限に対応するため、初回タップ
（`#start-overlay`）でAudioContextを生成・resumeしてから最も明るい星（シリウス）の
方向でテスト音を鳴らす。以後は「テスト音を鳴らす」ボタンで再生できる。
Playwrightでdevサーバーを起動しヘッドレスブラウザで確認: オーバーレイ表示→
タップ→テスト音再生→ドラッグ操作のフローでコンソールエラーがないことを確認した。
方向定位（左右・前後で聞こえ方が変わるか）自体は耳で聴く必要があるため、
実機・ヘッドホンでの確認はユーザーに依頼中。

## Phase 4: 明るさ・色から音響パラメータへのマッピング

- [x] 星の明るさ（vmag）→音量・余韻へのマッピング実装
- [x] 星の色（bv）→音色・音域へのマッピング実装
- [x] 複数の星を同時に鳴らして聴き比べ、明るさ・色の違いが聴感上わかるか確認
- [x] 実機確認: デプロイ→実機で複数星の聴き比べを確認

メモ: `src/audio.ts`の`SpatialAudio.playStar()`で実装。カタログ内でのvmag/bvの
相対位置（`setCatalogRange()`で記録）を基準に、vmag→音量(dB)・余韻(秒)、
bv→基音周波数(音域)・倍音の効かせ方(音色)を線形マッピング。数値検算では、
サンプル中最も明るい星(id=0, シリウス相当, vmag=-1.46)が最大音量・最長余韻に、
最も暗い星(id=19, vmag=1.33)が最小音量・最短余韻になることを確認。
最も青い星(id=19, bv=-0.24)が最高音域(880Hz)・最も明るい音色に、
最も赤い星(id=9, bv=1.85)が最低音域(440Hz)・最も抑えた音色になることも確認
（詳細はFindings.md参照）。「発音頻度」（星々が確率的に自動で鳴る仕組み）は
当初の想定通りPhase 6（確率的発音・生成的タイミング）に実装を回した。
UIに「テスト音を鳴らす（最も明るい星）」と「全ての星を聴き比べる」（20件を
0.15秒間隔でスケジュール再生）の2ボタンを追加。Playwrightでボタン操作の
コンソールエラーがないことを確認済み。実際の聴感（明るさ・色の違いが
聞き分けられるか）は実機・ヘッドホンでの確認をユーザーに依頼中。

追記（ユーザーフィードバック）: 「全ての星を聴き比べる」（0.15秒間隔）は
重なりが多くコード的に響くため、一つずつの違いを聴き取りたいときのために
「一個ずつ順番に鳴らす」ボタンを追加した（1.5秒間隔、前の星の余韻がおおよそ
収まってから次が鳴る）。用途によって聴き比べ方を選べるようにする、という
ユーザーの提案を反映。

## Phase 5: ペンタトニックスケールの適用

- [x] 半音階ではなくペンタトニック（等）音階へ音程を量子化
- [x] 多数の星が同時に鳴っても不協和になりにくいか確認
- [x] 実機確認: デプロイ→実機で確認

メモ: `src/audio.ts`の`buildPentatonicScale()`で、ルートA4=440Hzを基準にした
長調ペンタトニック（半音間隔0,2,4,7,9＝A-B-C#-E-F#、短2度・トライトーンを含まない）
の音を330〜990Hzの範囲で列挙し8音の音階を生成。Phase 4では連続値だった
bv→基音周波数のマッピングを、この8音のうち最も近い音へ丸める方式に変更した
（青いほど高い音、赤いほど低い音）。サンプル20件で検算したところ8音すべてが
使われ、370.0/440.0/493.9/554.4/659.3/740.0/880.0/987.8Hzのいずれかに量子化
されることを確認（音色の連続的な変化（brightness）はPhase 4のまま維持）。
「不協和になりにくいか」は聴感の判断のため、実機での確認をユーザーに依頼中。

## Phase 6: 確率的発音・生成的タイミング

- [x] 星の位置は固定のまま、発音タイミングを確率的にする実装
      （「星が瞬いているようなサウンドスケープ」）
- [x] 実機確認: デプロイ→実機で確認

メモ: `src/soundscape.ts`の`Soundscape`クラスで実装。星ごとに独立した
再帰的`setTimeout`で次回発音までの間隔をスケジュールし、間隔は
（明るさに応じた平均間隔）×（0.5〜1.5倍のランダムなジッター）で決める
（星の位置は固定、鳴るタイミングだけ生成的にする、というPlan.mdの方針通り）。
平均間隔は明るい星ほど短く（既定5秒）、暗い星ほど長く（既定30秒）マッピング。
星の位置は固定のままタイミングだけ生成的にする、というPlan.mdの方針通り。
20個の星の合計発音レートを事前に試算し、初期案（2〜12秒間隔）だと合計で
約2.7回/秒とかなり密集することが分かったため、5〜30秒間隔に調整して
全体で約1回/秒程度の落ち着いた瞬きになるようにした（詳細はFindings.md）。
「タップして音を有効にする」の直後に自動で開始する。Playwrightでサウンドスケープを
10秒間動かしコンソールエラーがないことを確認。頻度感が意図通り「瞬いている」
ように聞こえるかは実機での確認をユーザーに依頼中。

追記（ユーザーフィードバック）: 発音頻度を固定値ではなくスライダーで可変にしたい
という要望を受け、`Soundscape`に`density`倍率（既定1、範囲0.25〜4）を追加。
画面左上の「瞬きの頻度」スライダーで調整でき、`setDensity()`は変更時に実行中の
タイマーを組み直して即座に反映する。既定の間隔（Phase 7時点: 明るい星50秒〜
暗い星300秒）はdensity=1のときの値としてそのまま維持し、スライダーはこれに
対する倍率として作用する。

## Phase 7: フルカタログへの拡張（横展開）

- [x] 対象を約3等星まで（`bright_stars_v3.5.csv`全285件相当）に拡張
- [x] パフォーマンス（同時発音数、描画負荷）を確認し、必要なら間引き・最適化
- [x] 実機確認: デプロイ→実機（特にスマホ）でパフォーマンスを確認

メモ: `scripts/build-catalog.mjs`の`STAR_LIMIT`を`20`から`null`（全件）に変更し、
`public/data/stars.json`を285件で再生成（元CSVは285行全て、Plan.mdの「約3等星まで」
と一致：vmag最大3.49）。描画は星ごとに個別`Mesh`を作る方式だと285件でドローコールが
増えるため、`src/scene.ts`を単一`THREE.InstancedMesh`（星の数によらずドローコール1回）
へ変更。Playwrightでの実測ではrAFベースのFPSが約64（ほぼ上限）、ヘッドレスブラウザの
コンソールエラーもなし。星の数が増えると`Soundscape`（Phase 6）の合計発音レートも
比例して増える点は事前に把握していたので、20件時の間隔（5〜30秒）を星数比
（285/20≈14.25倍）で単純倍増するのではなく、285件の実際のvmag分布で改めて
合計レートを試算し、明るい星45秒〜暗い星270秒程度（採用値: 50秒〜300秒、
合計レート約1.16回/秒）に調整した（詳細はFindings.md）。テスト用の「聴き比べ」
ボタン（Phase 4・追記分）も285件全てを鳴らすと再生に数分かかってしまうため、
明るさ順に等間隔サンプリングした代表20件に対象を絞るよう変更した。
実機（特にスマホ）でのパフォーマンス確認はユーザーに依頼中。

## Phase 8: モバイル対応（端末の向き）

- [x] スマートフォンの向き（DeviceOrientation）で視点・聴取方向を操作できるようにする
- [ ] 実機確認: デプロイ→実機（スマホ）で端末の向きによる視点操作を確認

メモ: `src/device-orientation-controls.ts`に`DeviceOrientationControls`を新規実装
（three.jsが以前examplesで配布していた定番アルゴリズムを、依存を増やさず自前で
移植。alpha/beta/gamma+画面の向きからカメラのクォータニオンを求める）。
iOSは`DeviceOrientationEvent.requestPermission()`をユーザー操作内で呼ぶ必要が
あるため、その静的メソッドが存在する場合のみ呼び出す形にした（Android等では
そのまま利用できる）。`LookControls`（Phase 2のドラッグ操作）に`setEnabled()`を
追加し、端末の向き操作とドラッグ操作が同時に競合しないよう排他的に切り替える。
タッチ対応端末（`navigator.maxTouchPoints > 0`）でのみ「端末の向きで操作する」
トグルボタンを表示（デスクトップでは意味をなさないため）。Playwrightで
iPhone 13相当のデバイスエミュレーション（タッチ対応）を使い、ボタンの出現・
トグル動作・コンソールエラーなしを確認。ただし実際のセンサー入力は
ヘッドレス環境では再現できないため、視点が実際に端末の向きに追従するかは
実機での確認が必須。
実装の過程で、モバイル幅（390px）だとボタン群同士が重なるレイアウト崩れを
発見・修正した（「端末の向きで操作する」ボタンを頻度スライダーのパネルへ
まとめ、右下の星数ステータス表示を右上へ移動して幅を制限）。

## Phase 9: UI/UX仕上げ

- [ ] シンプルな説明・操作ガイドを追加（過剰な演出は追加しない）
- [ ] モバイル一画面に主要体験が収まるか確認
- [ ] 実機確認: デプロイ→実機で確認

## Phase 10: 教育目標との対応確認

- [ ] `Plan.md`記載の教育目標それぞれについて、UIのどの部分が対応しているかを
      明示的に確認する
- [ ] 対応が薄い目標があれば、機能追加ではなく既存要素の調整で対応できないか検討する
- [ ] 実機確認: 最終確認
