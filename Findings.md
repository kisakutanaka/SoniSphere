# Findings.md

開発中に得られた学術的・科学的な発見や検証結果のログ。
「なぜこの数値なのか」「公表値とどれだけ合っているか」など、実装の副産物として
得られる知見をここに追記する。一度作って終わりではなく、機能追加のたびに
関連する発見がないか振り返り、あれば都度追記する（指示がなくても自発的に行う）。

## Gaiaカタログ抽出データの妥当性（Phase 1）

`gaia_catalog/bright_stars_v3.5.csv`から明るさ上位20件を抽出したところ、
最も明るい星（id=0）はvmag=-1.46、ra≈101.29度、dec≈-16.72度だった。これは
実視等級-1.46等・赤経約6h45m・赤緯約-16.7度という、シリウスの実際の値と一致する。
赤経・赤緯から天球上の単位ベクトルへの変換（`scripts/build-catalog.mjs`の
`equatorialToUnitVector`）が正しく機能していることの傍証になる。

## 明るさ→音量マッピングに等級の対数性を利用（Phase 4）

天文学の実視等級(vmag)は対数尺度（等級差1で明るさ約2.512倍）であり、
音量のデシベル(dB)も対数尺度（人間の聴覚のラウドネス知覚にほぼ比例）である。
そのため`src/audio.ts`では、vmagをdBへ**線形**にマッピングしている
（`GAIN_DB_AT_BRIGHTEST`〜`GAIN_DB_AT_DIMMEST`の範囲に線形補間）。
対数×対数の単純な線形写像で済むため、途中に指数変換を挟む必要がなく、
かつ「等級差が一定なら聴感上のラウドネス差もおおよそ一定」という
直感的に自然な対応になる。

サンプル20件（vmag: -1.46〜1.33）での検算:
- 最も明るい星（id=0, vmag=-1.46）→ gainDb=-2.0dB, 余韻2.2秒
- 最も暗い星（id=19, vmag=1.33）→ gainDb=-20.0dB, 余韻0.7秒

## 色指数(B-V)→音域・音色マッピングの検算（Phase 4）

B-V色指数は値が小さい（負に近い）ほど青色・高温、大きいほど赤色・低温の星を表す。
`src/audio.ts`ではB-Vをカタログ内の相対位置に正規化し、基音周波数(440〜880Hz)と
倍音の効かせ方(0.7〜1.4)へ線形マッピングしている。

サンプル20件（bv: -0.24〜1.85）での検算:
- 最も青い星（id=19, bv=-0.24）→ 基音880Hz、明るい音色（brightness=1.40）
- 最も赤い星（id=9, bv=1.85）→ 基音440Hz、こもった音色（brightness=0.70）

色による音の高低・明暗の向きが意図（青=高く明るい音、赤=低くこもった音）通り
になっていることを確認した。

## ペンタトニックスケールへの量子化（Phase 5）

Phase 4まではB-Vから基音周波数を連続値でマッピングしていたが、多数の星が
同時に鳴ると任意の周波数比になり、短2度やトライトーンなど不協和な音程が
生じうる。Phase 5でルートA4=440Hzを基準にした長調ペンタトニック
（半音間隔0, 2, 4, 7, 9＝A, B, C#, E, F#。短2度・トライトーンを含まない5音音階）
の音のみを330〜990Hzの範囲で列挙し（370.0/440.0/493.9/554.4/659.3/740.0/
880.0/987.8Hzの8音）、B-Vが最も近い音へ丸められるようにした。サンプル20件
（B-V: -0.24〜1.85）では8音すべてが使用され、偏りなく音階全体に分布することを
確認した。

## 確率的発音の頻度試算（Phase 6）

星ごとに独立した間隔（明るさに応じて平均5〜30秒）でランダムに発音させる場合、
星の数が増えるほど「全体として何回/秒鳴るか」は各星の発音レート(1/平均間隔)の
合計になる。20個の星それぞれの平均間隔をvmagから計算し、その逆数を合計する
ことで全体の発音レートを事前に試算した。

- 当初案（明るい星2秒〜暗い星12秒）: 合計レート 約2.7回/秒（平均0.38秒に1回、
  かなり密集し常時音が重なる状態になる）
- 採用案（明るい星5秒〜暗い星30秒）: 合計レート 約1.0回/秒（平均1秒に1回、
  個々の星の音が聞き分けやすい落ち着いた瞬き方になる）

星の数（Phase 7でのフルカタログ拡張時は285個）が変わると合計レートも比例して
変わるため、対象を拡張する際はこの試算方法で間隔を再調整する必要がある。

## フルカタログ拡張時の発音レート再調整（Phase 7）

20件→285件（約14.25倍）への拡張にあたり、間隔を単純に14.25倍する（5〜30秒→
約71〜427秒）のではなく、285件の実際のvmag分布で改めて合計レートを試算した。
理由: 実際の恒星は暗い等級ほど数が多い（光度関数）ため、20件サンプル（上位の
明るい星のみ）と285件フルカタログとでは同じ等級範囲内でのvmagの分布密度が
大きく異なり、単純倍率では狙った合計レートにならない。

試算結果（285件、vmag範囲-1.46〜3.49）:
- 20件時と同じ間隔（明るい星5秒〜暗い星30秒）のまま285件に適用: 合計レート
  約11.6回/秒（約0.09秒に1回、音の洪水になる）
- 間隔を明るい星50秒〜暗い星300秒に拡大: 合計レート 約1.16回/秒
  （20件時の約1.0回/秒に近い、落ち着いた瞬き方を維持）

## 描画パフォーマンス（Phase 7）

星ごとに個別の`THREE.Mesh`を生成する実装（Phase 2〜6、最大20件）から、
`THREE.InstancedMesh`（星の数によらずドローコール1回）に変更して285件へ
拡張した。Playwright（ヘッドレスChromium）でのrAFベースのフレームレート実測は
約64fps（ディスプレイ上限相当）で、285件表示でも描画がボトルネックに
なっていないことを確認した。ヘッドレス環境の数値は実機のGPU性能を必ずしも
代表しないため、実機（特にスマートフォン）での体感確認は別途必要。
