# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 現在の状態

このリポジトリは企画段階であり、まだソースコード・ビルド設定は存在しない
（`package.json`等なし。バージョン管理も未初期化）。現状あるのは以下のみ:

- `Plan.md` — 企画書（コンセプト・教育目標・音マッピング案・設計方針）
- `science-app-framework.md` — ScienceMuseum系アプリ共通の開発フレームワーク
  （このアプリ固有ではなく、GWPlayer開発から一般化した進め方のテンプレート）
- `gaia_catalog/bright_stars_v3.5.csv` — Gaia由来の明るい恒星カタログ（生データ、286行）

実装がまだないため、ビルド/lint/testコマンドは存在しない。実装を始める際は
このセクションを更新し、実際のコマンドを追記すること。

## プロジェクト概要（SoniSphereとは）

全天の星空を「見る」のではなく「聴く」ことで体験する教育用Webアプリ。
Gaiaの恒星データから約3等星までの明るい恒星を全天球上に配置し、各星の方向から
音が聞こえるサラウンド音響として表現する。明るさ・色などの天文学的情報を
音量・音色・音程にマッピングする（詳細は`Plan.md`を参照）。

音への基本マッピング:

- 星の方向 → 3D空間上の音源方向
- 星の明るさ → 音量・余韻・発音頻度
- 星の色（B-V）→ 音色・音域
- 発音は常時ではなく確率的（星の位置は固定、発音タイミングは生成的）
- 和音の濁りを避けるため半音階ではなくペンタトニック等の音階を使用
  （最初のプロトタイプは「ペンタトニック + 星の色による音色変化」）

## 設計方針（このプロジェクトのデフォルト）

`science-app-framework.md`のScienceMuseum系共通方針を継承しつつ、`Plan.md`で
以下がSoniSphere固有の方針として明記されている。変更する場合は理由を検討すること。

- ブラウザのみで動作（GitHub Pages対応、サーバーサイド処理に依存しない）
- TypeScriptベース
- スマートフォン・PC両対応
- Web Audio APIによる3D音響、ヘッドホン体験を基本とする
- Gaiaデータは事前処理して軽量なJSONに変換し同梱する
  （ブラウザから直接Gaiaへ問い合わせない）
- 科学データと音の対応関係はできるだけ単純にする
- 音楽作品に寄りすぎず「星の性質を聴いている」ことを優先する
- UI・説明はシンプルに保ち、不要な機能や装飾を追加しない

## 開発の進め方（フレームワークより）

`science-app-framework.md`はこの手のアプリ開発で毎回使う型をまとめたもの。
今後の実装では以下を踏襲する。

**3点セットのドキュメント運用:**
- `CLAUDE.md`（本ファイル）— ほぼ変化しない指針
- `Step.md`（未作成）— 実装ロードマップ。「動くものが一つ増える」単位でフェーズ分割し、
  チェックボックスで進捗管理。実装後は各ステップに一言メモを追記する
- `Findings.md`（未作成）— 検証結果・科学的知見のログ。機能追加のたびに関連する
  発見がないか振り返り、指示がなくても自発的に追記する

実装を始める際はこの2ファイルを作成すること。

**進め方の要点:**
- まず1つの縦の筋（MVP）を全機能通しで動かしてから横展開する
  （例: まず少数の恒星だけで音響パイプライン全体を通す）
- 各フェーズの最後に実機（デプロイ→実際にアクセス）確認を入れる
- 自前実装の計算（座標変換、明るさ→音量マッピング等）は可能な限り公表値と
  突き合わせて検証し、差異があれば原因を特定してFindings.mdに記録する
- モックではなく実際にブラウザで動かして確認する（Playwright等）。
  状態を持つUI（再生・切り替え）はリスナーリークやレースコンディションに注意する

## データ

`gaia_catalog/bright_stars_v3.5.csv` は生のGaiaカタログ抽出データで、列は
`ra, dec, vmag, bv`（赤経・赤緯・視等級・B-V色指数）。これは前処理前の元データであり、
実装時にはこれをビルド時スクリプトで軽量なJSON（アプリ同梱用）へ変換する方針
（`Plan.md`の「設計方針」参照）。ブラウザ側でCSVを直接パースしない設計にすること。
