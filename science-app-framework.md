# 科学教育Webアプリ 共通開発フレームワーク

題材（重力波、天体軌道、特殊相対性理論、電磁波...）によらず、Claudeと一緒に
ScienceMuseum系の教育用Webアプリを作るときに毎回使える共通の型をまとめたもの。
GWPlayer（重力波プレイヤー）の開発を通じて実際に機能した進め方を一般化した。

新しいアプリを始めるときは、このファイルの内容をベースにそのアプリ用の
CLAUDE.mdを書き起こすところから始めるとよい。

---

## 1. ドキュメント構成（3点セット）

プロジェクトルートに以下の3つのMarkdownを置き、それぞれ役割を分ける。

- **CLAUDE.md** — コンセプト・教育目標・設計方針など、開発中ほぼ変化しない指針。
  Claude Codeが起動時に自動で読み込む。
- **Step.md** — 実装ロードマップ。企画を「動くものが一つ増える」粒度のフェーズに
  分割し、チェックボックスで進捗管理する。実装後は各ステップに一言メモ
  （何を作ったか、何が分かったか）を書き足していく。
- **Findings.md** — 開発中に得られた学術的・科学的な発見や検証結果のログ。
  「なぜこの数値なのか」「公表値とどれだけ合っているか」等、実装の副産物として
  得られる知見を都度追記する。一度作って終わりではなく、機能追加のたびに
  関連する発見がないか振り返り、あれば追記する（ユーザーから明示の指示がなくても
  自発的に行う）。

## 2. 設計方針のデフォルト

特に理由がなければ以下を出発点にする。題材ごとの事情があれば個別に上書きする。

- **ブラウザのみで完結**させる（サーバーサイド処理に依存しない静的サイト。
  GitHub Pages等で無料ホスト可能な構成を前提にする）
- **モバイル・タッチ操作を前提**にUI/パフォーマンスを設計する
  （展示端末・来館者のスマホでの利用を想定）
- **教育効果を最優先**する。機能追加や技術的な凝り方より、学習目標への
  貢献度で判断する
- **UIはできるだけシンプルに**し、不要な機能や華美な装飾を追加しない
- 技術選定に迷ったら「ブラウザのみで完結」「モバイルで動く」「教育効果」を
  優先基準にする

## 3. 実装の進め方

- **まず一つの具体例で全機能を縦に通す**（MVP）。複数データセット/複数パターン
  対応などの横展開は、縦の筋が動くことを確認してから着手する
  （例: GWPlayerでは最初は1イベント[GW150914]だけで全パイプラインを実装し、
  後から複数イベント選択機能を追加した）
- 各フェーズは「動くものが一つ増える」単位で区切り、都度動作確認する
- **各フェーズの最後に実機確認ステップを入れる**（デプロイ→実機でアクセス→
  確認、をStep.mdに明記する）。まとめて最後にやるのではなく、フェーズごとに
  早期にデプロイして確認するサイクルを回す
- ユーザーからの調整依頼（「もう少し縮めて」「軸ラベルを入れて」等）は
  都度Step.mdか実装コメントに反映し、後から経緯を追えるようにする
- 機能追加の途中でも「これはStep.mdに追記した方がいいか」「これは
  Findings.mdに残すべき発見か」を自発的に判断し、都度反映する

## 4. データ・API取扱い

- 重い元データ（観測データ、シミュレーション結果等）はビルド時のNode.js
  スクリプトで前処理し、軽量なデフォルトデータのみをアプリにバンドルする
  （ランタイムはブラウザのみで完結させる）
- 複数データセットに対応する場合は、アプリには**カタログ情報（メタデータ）
  だけ**を持たせ、実データはユーザー操作時に公開APIから直接オンデマンド取得する
  （CORSが全許可されている公開データAPIを優先的に探す）
- 外部APIの癖（バージョニング、フィールドの信頼性、命名規則の揺れ等）は
  仮定で実装せず、実際に叩いて確認してから実装に反映する
- 同じ対象に複数バージョン/改訂がある場合は「どのバージョンを正とするか」の
  解決ロジックを明示的に持つ（例: 最新のうち実データが存在するものを採用）

## 5. 科学的正しさの検証

- 自前実装した計算・推定式は、可能な限り**公表値や公式APIの値と突き合わせて
  検証**する（有名な事実・論文記載値との整合性チェック等）
- 差異が出た場合は「なぜ違うのか」を理解して記録する（丸め誤差、定義の違い、
  座標系/フレームの違い等）。差異を無視せず、原因を特定してから許容できるか
  判断する
- 定量的に検証できるものは定量的に確認する（数値シミュレーションなら
  既知の恒等式・保存則で検算する等）
- 検証によって得られた知見・数値・比較表はFindings.mdに残す

## 6. テスト・確認の型

- モックではなく**実際にアプリを起動して**ブラウザで動かして確認する
  （Playwright等）。可能であれば実データ・実APIに対して検証する
  （モックにすると発見できないAPIの癖・実データの癖がある）
- スクリーンショット・コンソールログ・pageerrorを能動的に確認し、
  疑わしい挙動は自分で深掘りして原因を特定する（ユーザーに指摘される前に
  見つける）
- 状態を持つUI（切り替え・再生等）は、切り替えを繰り返すテストで
  リスナーリークやレースコンディションがないか確認する
- テストスクリプト自体の判定条件が緩すぎて偽陽性を生んでいないか、
  結果が想定より速すぎる/遅すぎるときは疑う

## 7. UIの型

- 「体験」（結果を直感的に見る/聴くデモ）と「仕組みの理解」（ステップごとの
  解説）を分離した構成が有効だった。まず結果を体感してもらい、下にスクロール
  すると仕組みの解説が続く、という流れ
- 選択肢UI（プルダウン等）は、選んだ後に出る説明文だけでなく、
  **選ぶ前の選択肢自体にも短いヒントを入れる**と親切
  （ネイティブ`<select>`なら`<option>`のテキストに埋め込む）
- モバイル一画面に収める意識（スクロールなしで主要な体験ができるか）を
  早めに確認する

## 8. 教育目標との対応付け

- 企画書に書かれた教育目標を、実装の終盤で「UIのどの部分がどの目標に
  対応しているか」を明示的に確認するフェーズをStep.mdに設ける
- 機能や演出を足すかどうか迷ったら、企画書の教育目標に立ち返って判断する
